<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>ArchLinux</title>
    <url>/2021/11/17/ArchLinux/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>近来无事，准备将ArchLinux安装至自己的机子上作为备用系统使用，<br>也是用于更好地学习Linux系统</p>
<hr>
<h2 id="正文"><a href="#正文" class="headerlink" title="正文"></a>正文</h2><h3 id="ArchLinux的安装"><a href="#ArchLinux的安装" class="headerlink" title="ArchLinux的安装"></a>ArchLinux的安装</h3><blockquote>
<p>由于个人精力有限，因此其安装过程针对一定的安装环境，实际操作请对照<a href="https://wiki.archlinux.org/title/Main_page_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)">官方文档</a>进行安装</p>
</blockquote>
<p>其他相关链接：  </p>
<ol>
<li><a href="https://archlinux.org/">ArchLinux社区</a></li>
<li><a href="https://www.jianshu.com/p/e48bff8c94cc">一些Linux启动流程的解析</a></li>
</ol>
<h4 id="什么是UEFI，BIOS"><a href="#什么是UEFI，BIOS" class="headerlink" title="什么是UEFI，BIOS"></a>什么是UEFI，BIOS</h4><p>BIOS(基本输入输出系统，Basic I&#x2F;O System)，是固化在计算机ROM上的一组程序，这组程序如其名字一样，作为操作系统启动前的步骤为操作系统的正式启动提供：  </p>
<ol>
<li>加电自检，检测CPU，计时芯片等各种计算机必需设备</li>
<li>枚举设备，初始化各设备和设备资源</li>
<li>从CMOS(可读写的RAM芯片，存储一些硬件配置和用户设定参数)中读取数据并进行有关系统设置<br>等功能</li>
</ol>
<p>UEFI(Unified Extensible Firmware Interface，统一可拓展固件接口)，其作为一个硬件接口，可用于系统启动，维护的硬件支持。</p>
<p>UEFI的前身是EFI，作为BIOS方案的竞争者，由于早期没有如UEFI一样的接口标准，导致各个厂家的EFI千差万别。</p>
<p>使用了UEFI固件支持的BIOS提供更多的功能如：  </p>
<ol>
<li>支持2TB以上的硬盘</li>
<li>支持GPT分区(传统的MBR格式最多支持四个主分区，若要更多的分区需要逻辑分区)等</li>
</ol>
<p>同时UEFI并没有传统BIOS的自检流程(传统启动方式称为<strong>Legacy</strong>)，因此可以提供更为快速的启动速度。</p>
<h4 id="什么是文件系统-filesystem"><a href="#什么是文件系统-filesystem" class="headerlink" title="什么是文件系统(filesystem)"></a>什么是文件系统(filesystem)</h4><p>文件系统的概念针对我们日常生活中所使用的各种存储设备如硬盘，USB，光盘等。</p>
<p>无论是我们日常所编写的文本文档，还是计算机程序，抑或是操作系统本身，都是一个<strong>文件</strong>，存储在我们使用的存储设备当中。</p>
<p>但是存储设备本身并没有特定的结构，整体就是存储单元的集合。而文件的调用，操作系统的引导都需要一种特定的结构去找到存储设备中对应的<strong>文件</strong>进行执行，例如索引结构等。</p>
<p>因此我们需要各种不同组织文件的方式，这就是<strong>文件系统</strong>。</p>
<p><strong>文件系统</strong>提供了各种文件在硬盘上的组织、存放方式，规定了文件本身的属性如文件名最大长度等。同时将数据结构的记录存储至存储设备上，使存储设备逻辑上具有所谓的<strong>文件系统</strong></p>
<h4 id="什么是分区表-MBR-GPT"><a href="#什么是分区表-MBR-GPT" class="headerlink" title="什么是分区表(MBR,GPT)"></a>什么是分区表(MBR,GPT)</h4><p>现今存储设备的容量已经大大提高，1TB的硬盘已经变得司空见惯。</p>
<p>而我们实际生活使用的过程中，由于各种需要，常对硬盘进行分区。</p>
<p>比如重要文件存放至分区1，其余文件存放至分区2。又或者系统1(作为一个文件)存放至分区3，系统2存放至分区4。</p>
<p>因此分区表就是为了对硬盘进行分区(逻辑意义上)，并提供分区索引，分区信息存储等功能。</p>
<p>分区表存放于每一个分区的开始部分，提供了该分区的起始扇区，终止扇区，文件系统，下一个分区的索引等。</p>
<p>mbr格式的分区表由于其所占存储空间有限，最多只能支持2TB的存储空间，且每一个设备最多划分出4个主分区。</p>
<p>超出4个需要使用逻辑分区支持。</p>
<p>而gpt格式的分区表存储空间更大，因此可以提供更多的信息，支持9.2ZB的存储空间，且可以存储更多的分区信息而无需划分出逻辑分区。</p>
<h4 id="什么是grub，syslinux（BootLoader）"><a href="#什么是grub，syslinux（BootLoader）" class="headerlink" title="什么是grub，syslinux（BootLoader）"></a>什么是grub，syslinux（BootLoader）</h4><p>Grub和Syslinux(只能启动Linux系统)等都是一种系统引导程序(Bootloader)，本身也作为一个文件存储于设备之中。</p>
<p>UEFI或者BIOS会识别对应的分区并加载对应的程序，当BootLoader加载之后可以根据已有的配置加载已有的系统</p>
<p>类似Grub和Elilo等的BootLoader有针对UEFI和BIOS有相应的版本，安装时根据自己主板的型号进行选择。</p>
<p>需要注意的是，BIOS版本的BootLoader存放于boot分区(fdisk)，UEFI版本的BootLoader存放于EFI分区(EFI文件系统，EFI System)</p>
<h4 id="现在来安装一个ArchLinux"><a href="#现在来安装一个ArchLinux" class="headerlink" title="现在来安装一个ArchLinux"></a>现在来安装一个ArchLinux</h4><p>安装时间：2021.09.26</p>
<p>本机安装环境：   </p>
<ol>
<li>系统存储于外置硬盘</li>
<li>通过U盘启动盘安装</li>
<li>x86_64芯片架构</li>
</ol>
<p><strong>下载ArchLinux镜像</strong></p>
<p><a href="https://archlinux.org/download/">下载地址：官网</a></p>
<p><strong>制作启动盘</strong></p>
<p><em>Windows下：</em></p>
<p>可以使用<a href="https://www.diskgenius.cn/">DiskGenius</a>或者Windows自带的<strong>设备管理器</strong>进行分区</p>
<p>分区完毕后可以使用<a href="https://cn.ultraiso.net/">UltralISO</a>进行启动盘的制作</p>
<p><em>Linux下：</em>  </p>
<blockquote>
<p>dd if&#x3D;(source file name) of&#x3D;(target file name) (Options)</p>
</blockquote>
<p>使用<code>dd</code>命令将镜像文件拷贝至对应U盘设备</p>
<p><strong>根据主板型号进入BIOS&#x2F;UEFI并选择U盘启动</strong></p>
<p>通常使用<code>F2</code>，<code>Esc</code>进入，不同型号的主板可能有所差异，请以主板制造商说明为准。</p>
<p><strong>存储设备分区</strong></p>
<p>*注：若无特殊说明，<code>/dev/xxx</code>统一为与挂载，分区等操作对应的设备分区</p>
<p>使用<code>fdisk -l</code>查看各个存储设备和编号</p>
<p>若为UEFI启动：使用<code>gdisk /dev/xxx(设备编号)</code>命令进行gpt格式的分区</p>
<p>同时需要注意预留500MB以上的EFI分区，swap分区建议为内存大小或者两倍内存大小</p>
<p>若为BIOS启动：使用<code>fdisk /dev/xxx(设备编号)</code>命令进行mbr格式的分区</p>
<p>需要启动一个boot分区</p>
<p><strong>初始化文件系统</strong></p>
<ul>
<li><code>mkfs.vfat /dev/xxx</code> 或者 <code>mkfs.fat -F32 /dev/xxx</code> 设置EFI分区（若需要的话）</li>
<li><code>mkfs.ext4 /dev/xxx</code> 对需挂载根目录等的分区进行<code>ext4</code>文件系统的格式化</li>
<li><code>mkswap /dev/xxx</code> 设置交换分区</li>
<li><code>swapon /dev/xxx</code> 开启交换分区</li>
</ul>
<p><strong>挂载分区</strong></p>
<p>由于我们需要从U盘进入我们安装的系统进行操作，因此需要先进行分区的挂载</p>
<p>我们挂载至<code>/mnt</code>分区下，此分区适用于挂载其他系统并对其进行操作。</p>
<p>由于我们刚格式化分区，因此系统以及相关组件并未被安装，并没有类似<code>/home</code>等目录</p>
<p>因此我们手动建立文件夹并进行相应的挂载</p>
<ul>
<li><code>mount /dev/xxx /mnt</code> 挂载根分区</li>
<li><code>mkdir /mnt/boot</code> 建立boot目录，若为BIOS&#x2F;MBR启动，此时可以进行<code>mount /dev/xxx /mnt/boot</code></li>
<li><code>mkdir /mnt/boot/EFI</code> 建立EFI目录，若为UEFI&#x2F;GPT启动，此时可以进行<code>mount /dev/xxx /mnt/boot/EFI</code></li>
<li><code>mkdir ...</code></li>
<li><code>mount ...</code></li>
<li>直到全部分区正确挂载完毕</li>
</ul>
<p><strong>下载基本Linux系统和必要组件</strong></p>
<p>格式化的分区并没有基本的Linux系统和组件，因此需要下载</p>
<p>如果下载过程中遇到下载速度慢或者中断等问题，请查看源设置：</p>
<p>打开 <code>/etc/pacman.d/mirrorlist</code> 并添加 </p>
<ul>
<li>兰大镜像源<br><code>Server = https://mirror.lzu.edu.cn/archlinux/$repo/os/$arch</code>  </li>
<li>清华镜像源<br><code>Server = https://mirrors.tuna.tsinghua.edu.cn/archlinux/$repo/os/$arch</code></li>
</ul>
<p>或者其他国内镜像源</p>
<p>然后刷新源</p>
<blockquote>
<p>pacman -Syy</p>
</blockquote>
<p>下载基本系统和组件</p>
<blockquote>
<p>pacstrap &#x2F;mnt base linux linux-firmware</p>
</blockquote>
<p><code>pacstrap</code> 是Arch提供的脚本，用于安装一些基础软件</p>
<p>实际使用时可以再安装<code>vim</code> <code>nano</code>等文本编辑器软件方便之后的配置文件的更改</p>
<p><strong>生成fstab</strong></p>
<p><em>fstab</em>用于在加载linux系统时提供分区挂载的信息，缺少此文件或者配置出错将会导致系统无法进入甚至崩溃。</p>
<p><em>ArchLinux</em>下提供了自动生成目录表的工具<code>genfstab</code></p>
<p>具体使用：</p>
<blockquote>
<p>genfstab -U &#x2F;mnt &gt;&gt; &#x2F;mnt&#x2F;etc&#x2F;fstab</p>
</blockquote>
<p><em>-U</em> 参数表示使用设备的<strong>UUID</strong>进行挂载</p>
<p>UUID是一个设备的绝对地址(理论上会产生冲突，但实际情况下发生冲突的可能性可以忽略不记)</p>
<p><em>&gt;&gt;</em> 表示将前部分的输出写入后部分对应的文件</p>
<p><strong>从U盘进入系统</strong></p>
<blockquote>
<p>arch-chroot &#x2F;mnt</p>
</blockquote>
<p><strong>设置时间</strong></p>
<p><em>设置时区</em>  </p>
<blockquote>
<p>ln -sf &#x2F;usr&#x2F;share&#x2F;zoneinfo&#x2F;Asia&#x2F;Shanghai &#x2F;etc&#x2F;localtime</p>
</blockquote>
<p><em>同步时间</em>  </p>
<blockquote>
<p>hwclock –systohc</p>
</blockquote>
<p>可以使用<a href="https://wiki.archlinux.org/title/Network_Time_Protocol_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)">ntp</a>进行时间的同步。</p>
<p><strong>修改主机名</strong></p>
<p>建立 <strong>&#x2F;etc&#x2F;hostname</strong> 文件写入主机名</p>
<p><strong>更改语言</strong></p>
<p>修改 <strong>&#x2F;etc&#x2F;locale.gen</strong> 文件将需要的字符集前的注释 <em>#</em> 删去</p>
<p>建立 <strong>&#x2F;etc&#x2F;locale.conf</strong> 文件写入</p>
<blockquote>
<p>LANG&#x3D;en_US.UTF-8</p>
</blockquote>
<p>*官方wiki中推荐的是先使用<code>en_US.UTF-8</code>，以便终端界面能够正常显示</p>
<p>运行 <code>locale-gen</code> 使语言设置生效</p>
<p><strong>修改网络设置</strong></p>
<p>修改 <strong>&#x2F;etc&#x2F;hosts</strong> 文件写入</p>
<blockquote>
<p>127.0.0.1 localhost</p>
</blockquote>
<blockquote>
<p>::1       localhost</p>
</blockquote>
<blockquote>
<p>127.0.0.1 主机名.localdomain 主机名 </p>
</blockquote>
<p><strong>安装网络相关的包</strong></p>
<p>由于ArchLinux秉持轻量化，因此基本系统中并未包括我们常用的网络资源管理相关的软件，需要自行下载。</p>
<blockquote>
<p>pacman -S iw wpa_supplicant dialog dhcpcd</p>
</blockquote>
<p>*dhcpcd 用于设置ip，如果使用 <code>ip</code> <code>wpa_supplicant</code> <code>dhcpcd</code> 方式配置网络需要安装</p>
<p><strong>安装引导程序(bootloader)</strong></p>
<p><em>BIOS&#x2F;MBR</em></p>
<blockquote>
<p>grub-install –target&#x3D;i386-pc &#x2F;dev&#x2F;xxx (这里为boot分区挂载的设备)  </p>
</blockquote>
<blockquote>
<p>grub-mkconfig -o &#x2F;boot&#x2F;grub&#x2F;grub.cfg 根据grub配置更改grub相应参数(可自定义)</p>
</blockquote>
<p><em>UEFI&#x2F;GPT</em></p>
<blockquote>
<p>pacman -S grub efibootmgr</p>
</blockquote>
<blockquote>
<p>grub-install –target&#x3D;x86_64-efi –efi-directory&#x3D;&#x2F;boot&#x2F;EFI –bootloader-id&#x3D;grub</p>
</blockquote>
<blockquote>
<p>grub-mkconfig -o &#x2F;boot&#x2F;grub&#x2F;grub.cfg</p>
</blockquote>
<p><strong>initramfs生成</strong></p>
<p>由于Linux系统的启动需要同时启动必要的组件，而某些组件之间又相互依赖，或者存在兼容性问题。</p>
<p>为了解决这些问题，Linux在真正的系统启动前需要启动<code>initramfs</code>，即initram file system，用于提供一个用户态的环境以完成内核不易完成的操作，比如加载必要的模块等</p>
<p>使用<code>mkinitcpio -P</code>打包initramfs</p>
<p><em>关于&#x2F;usr分区独立进行挂载的问题</em></p>
<p>在目前的ArchLinux中，<code>/sbin</code> 链接至 <code>/usr/sbin</code> 当我们将<code>/usr</code>分区独立挂载后，会导致initramfs无法找到相应的文件导致启动失败</p>
<p>解决方法：  </p>
<ol>
<li>修改<code>/etc/fstab</code>文件找到<code>/usr</code>分区一行，将<code>pass</code>参数设置为0</li>
<li>修改<code>/etc/mkinitcpio.conf</code>文件，在<em>HOOK</em>中添加shutdown和usr</li>
</ol>
<p>然后重新打包initramfs</p>
<blockquote>
<p>mkinitcpio -P</p>
</blockquote>
<p><strong>修改root密码</strong></p>
<blockquote>
<p>passwd</p>
</blockquote>
<p><strong>退出当前系统</strong></p>
<blockquote>
<p>exit</p>
</blockquote>
<p><strong>解除挂载</strong></p>
<blockquote>
<p>umount -R &#x2F;mnt (从根目录递归解除挂载)</p>
</blockquote>
<p><strong>重启进入系统</strong></p>
<blockquote>
<p>reboot</p>
</blockquote>
<h3 id="ArchLinux系统的配置"><a href="#ArchLinux系统的配置" class="headerlink" title="ArchLinux系统的配置"></a>ArchLinux系统的配置</h3><h4 id="网络配置"><a href="#网络配置" class="headerlink" title="网络配置"></a>网络配置</h4><p>*使用 <code>ip+wpa_supplicant+dhcpcd</code> 方式</p>
<p><strong>启用网络接口</strong></p>
<blockquote>
<p>lspci | grep Network</p>
</blockquote>
<p>查看网卡设备是否能被识别到</p>
<blockquote>
<p>ip link</p>
</blockquote>
<p>查看网络借口名称</p>
<p>*<code>lo</code>表示本地，<code>wl</code>开头表示无线网络接口，<code>en</code>开头表示有线网络接口</p>
<blockquote>
<p>ip link set 接口名称 up</p>
</blockquote>
<p>启用指定网络接口</p>
<p><strong>使用wpa_supplicant连接无线网络</strong></p>
<p>建立 <code>/etc/wpa_supplicant/wpa_supplicant.conf</code> 文件，写入</p>
<blockquote>
<p>ctrl_interface&#x3D;&#x2F;run&#x2F;wpa_supplicant</p>
</blockquote>
<blockquote>
<p>update_config&#x3D;1</p>
</blockquote>
<p>查看网络设备<code>ifconfig</code></p>
<p>*本人的网络设备是wlan0</p>
<p>根据配置设置wpa_supplicant</p>
<blockquote>
<p>wpa_supplicant -B -i wlan0 -c &#x2F;etc&#x2F;wpa_supplicant&#x2F;wpa_supplicant.conf</p>
</blockquote>
<p>网络设置</p>
<p>*一下为需要执行的命令，具体参数请根据自己的网络状态配置</p>
<blockquote>
<p>wpa_cli  </p>
<blockquote>
<p>scan<br>scan_results<br>add_network<br>set_network 0 ssid “MYSSID”<br>set_network 0 psk “passphrase”<br>enable_network 0<br>save_config  </p>
</blockquote>
</blockquote>
<p>由此配置完毕，之后可以于<code>/etc/wpa_supplicant/wpa_supplicant.conf</code>修改网络配置</p>
<blockquote>
<p>systemctl enable wpa_supplicant.service</p>
</blockquote>
<p>启用wpa_supplicant，当存在网络时自动连接，同时启用dhcpcd(<code>systemctl enable dhcpcd</code>)</p>
<p><strong>dhcpcd设置ip</strong></p>
<blockquote>
<p>dhcpcd 接口名称</p>
</blockquote>
<p>自动获取ip</p>
<h4 id="GRUB引导多系统"><a href="#GRUB引导多系统" class="headerlink" title="GRUB引导多系统"></a>GRUB引导多系统</h4><p><em>UEFI&#x2F;GPT下引导Windows</em></p>
<p>查看Win10启动分区(EFI分区)UUID，首先使用<code>fdisk -l</code>查看是哪个分区，比如是<code>/dev/sda1</code></p>
<p>使用<code>blkid /dev/sda1</code>查看分区的UUID</p>
<p>修改 <code>/boot/grub/grub.cfg</code> 文件</p>
<p>找到 <code>### BEGIN /etc/grub.d/30_os-prober</code> </p>
<p>添加Windows启动项</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">menuentry &#x27;Microsoft Windows 10&#x27;  &#123;</span><br><span class="line">	insmod part_gpt</span><br><span class="line">	insmod fat</span><br><span class="line">	insmod chain ## 链式加载</span><br><span class="line">	search --fs-uuid --no-floppy --set=root XXXX-XXXX ## EFI分区的UUID</span><br><span class="line">	chainloader ($&#123;root&#125;)/EFI/Microsoft/Boot/bootmgfw.efi</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>重启进入Grub引导界面查看效果</p>
<p>*如果没有发现Windows启动项，请试着修改 <code>/etc/default/grub</code> 文件</p>
<p>在文件中添加  </p>
<blockquote>
<p>GRUB_DISABLE_OS_PROBER&#x3D;false  </p>
</blockquote>
<p>之后重新配置grub(已被发现则无需配置)</p>
<ol>
<li>在Arch下是<code>grub-mkconfig -o /boot/grub/grub.cfg</code></li>
<li>在一些系统下是<code>update-grub</code></li>
</ol>
<p>请以安装的系统提供的grub文档为准</p>
<h4 id="安装音频支持"><a href="#安装音频支持" class="headerlink" title="安装音频支持"></a>安装音频支持</h4><p>*本人对音频安装需要解决：声卡内核驱动的安装，解除声卡静音，设置默认声卡</p>
<p><strong>声卡内核驱动的安装</strong></p>
<p>使用 <code>高级Linux声音体系（Advanced Linux Sound Architecture，ALSA）</code> 提供声卡的内核驱动</p>
<blockquote>
<p>pacman -S alsa-utils</p>
</blockquote>
<p>安装软件包，用来设置声音</p>
<p><strong>解除声卡静音</strong></p>
<blockquote>
<p>alsamixer</p>
</blockquote>
<p>进入声音管理界面调节声音，MM表示静音，使用<code>m</code>命令接触静音</p>
<blockquote>
<p>speaker-test -c 8</p>
</blockquote>
<p>检查是否正常输出声音，若没有声音输出或错误，可能是默认声卡配置的原因</p>
<blockquote>
<p>aplay -L | grep :CARD</p>
</blockquote>
<p>获取声卡的信息，<code>:</code>后 <code>=</code>前的名称为声卡的名称</p>
<blockquote>
<p>speaker-test -D default:名称 -c 8</p>
</blockquote>
<p>由上面的信息指定使用声卡名称进行测试</p>
<p><strong>设置默认声卡</strong></p>
<p>我们通过上述步骤获取声卡信息，其中包括声卡的编号，此时我们需要设置默认的声卡</p>
<p>建立 <code>/etc/asound.conf</code> 或者 <code>~/.asoundrc</code> 文件，在文件中键入</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">defaults.pcm.card 编号</span><br><span class="line">defaults.ctl.card 编号</span><br></pre></td></tr></table></figure>

<p>保存退出，再次测试可以更改默认声卡</p>
<p>可以安装图形化音频调节<code>kmix</code></p>
<p>目前我使用的音量调节程序：</p>
<p><code>alsamixer</code> - cli界面调节音量</p>
<p><code>amixer</code> - amixer set Master 50 , 调节主音量为50</p>
<p><code>kmix</code> - 图形化界面调节音量</p>
<h4 id="使linux支持挂载ntfs文件系统-微软使用"><a href="#使linux支持挂载ntfs文件系统-微软使用" class="headerlink" title="使linux支持挂载ntfs文件系统(微软使用)"></a>使linux支持挂载ntfs文件系统(微软使用)</h4><blockquote>
<p>pacman -S ntfs-3g</p>
</blockquote>
<p>安装之后连接对应存储设备，可以直接使用<code>mount</code>命令挂载</p>
<h4 id="安装中文输入法"><a href="#安装中文输入法" class="headerlink" title="安装中文输入法"></a>安装中文输入法</h4><p>首先安装输入法引擎，然后再安装对应语言的输入法。</p>
<blockquote>
<p>pacman -S fcitx</p>
</blockquote>
<p>安装fcitx输入法引擎</p>
<blockquote>
<p>pacman -S fcitx-sunpinyin</p>
</blockquote>
<p>安装一个中文输入法</p>
<blockquote>
<p>pacman -S fcitx-im</p>
</blockquote>
<p>安装输入法组件，以支持各种应用</p>
<blockquote>
<p>pacman -S fcitx-configtool</p>
</blockquote>
<p>图形化设置fcitx</p>
<h4 id="同步时间"><a href="#同步时间" class="headerlink" title="同步时间"></a>同步时间</h4><blockquote>
<p>pacman -S ntp</p>
</blockquote>
<p>安装ntp软件包</p>
<blockquote>
<p>ntpd -u ntp:ntp</p>
</blockquote>
<p>使用ntp</p>
<blockquote>
<p>ntpq -p</p>
</blockquote>
<p>启动时启用ntpd，用来同步时间</p>
<h3 id="ArchLinux系统的美化"><a href="#ArchLinux系统的美化" class="headerlink" title="ArchLinux系统的美化"></a>ArchLinux系统的美化</h3><h4 id="grub-theme"><a href="#grub-theme" class="headerlink" title="grub theme"></a>grub theme</h4><p>grub作为引导程序，支持定制化，因此可以制作个性化的启动页面主题。</p>
<p>grub主题资源：  </p>
<ol>
<li><a href="https://gnome-look.org/">Gnome-look</a></li>
<li><a href="https://opendesktop.org/">Opendesktop.org</a></li>
</ol>
<p>自定义安装：  </p>
<ol>
<li>首先将主题文件放置到系统grub主题文件夹中</li>
<li>Arch下可以是<code>/boot/grub/themes/</code>，也可以是<code>/usr/share/grub/themes/</code></li>
<li>修改 <code>/etc/default/grub</code> 文件，修改<code>GRUB_THEME=对应主题目录/themes.txt</code></li>
<li>重启系统查看效果</li>
</ol>
<h4 id="中文字体的支持和安装"><a href="#中文字体的支持和安装" class="headerlink" title="中文字体的支持和安装"></a>中文字体的支持和安装</h4><p>我们在<code>/etc/locale.gen</code>指定的是支持的字符集，但是除了字符集，我们还需要图形码来支持正常显示文字，也就是安装对应的字体。</p>
<p>在<a href="https://wiki.archlinux.org/title/Fonts_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)#%E4%B8%AD%E6%96%87%E5%AD%97">Wiki ArchLinux的字体列表</a>中，我们可以查看相应的字体并进行查看。</p>
<p>这里我下载安装两种字体，具体的应用会使用不同的字体。</p>
<blockquote>
<p>pacman -S wqy-zenhei adobe-source-han-sans-otc-fonts</p>
</blockquote>
<p>使用<code>fc</code>系列命令管理字体(请自行研究)</p>
<hr>
<h2 id="资料引用"><a href="#资料引用" class="headerlink" title="资料引用"></a>资料引用</h2><ol>
<li><a href="https://blog.csdn.net/alais/article/details/5129005">什么是UEFI和BIOS</a></li>
<li><a href="https://baijiahao.baidu.com/s?id=1627358879605845999&wfr=spider&for=pc">简单分析UEFI</a></li>
<li><a href="https://baike.baidu.com/item/%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F/4827215">文件系统</a></li>
<li><a href="http://linux.vbird.org/">鸟哥的Linux私房菜</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/99448314">ArchLinux安装(UEFI)</a></li>
<li><a href="https://wiki.archlinux.org/title/Mkinitcpio_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)">ArchLinux Wiki上的mkinitcpio解释</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/94331255">Linux Grub引导程序的主题美化</a></li>
<li><a href="https://wiki.archlinux.org/title/Wpa_supplicant_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)">ArchLinux Wiki上的wpa_supplicant解释</a></li>
<li><a href="https://blog.csdn.net/weixin_43669969/article/details/110820419">Grub双系统配置</a></li>
<li><a href="https://clinux.co/d/167-os-prober-will-not-be-executed-to-detect-other-bootable-partitions">os-prober错误配置导致多系统引导出错</a></li>
<li><a href="https://wiki.archlinux.org/title/Advanced_Linux_Sound_Architecture_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)#%E8%AE%BE%E7%BD%AE%E9%BB%98%E8%AE%A4%E5%A3%B0%E5%8D%A1">ArchLinux Wiki上的声音设备配置教程</a></li>
<li><a href="https://wiki.archlinux.org/title/Fcitx_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)">ArchLinux Wiki上输入法的安装</a></li>
<li><a href="https://wiki.archlinux.org/title/Network_Time_Protocol_daemon_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)">ArchLinux Wiki上时间同步程序ntp的安装</a></li>
</ol>
]]></content>
      <tags>
        <tag>Linux学习与技术</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker</title>
    <url>/2021/11/17/Docker/</url>
    <content><![CDATA[<blockquote>
<p>此为docker&#x2F;singularity使用方法的一些笔记，个人记录使用</p>
</blockquote>
<h2 id="docker三要素"><a href="#docker三要素" class="headerlink" title="docker三要素"></a>docker三要素</h2><ul>
<li>仓库：镜像仓库，镜像的存储库</li>
<li>镜像：系统镜像等，封装了应用的环境、信息，相当于容器（实例）的类</li>
<li>容器：由系统镜像（静态配置）生成的应用，可以直接运行镜像中的应用或者使用交互式（类似虚拟机，但占用资源更少）</li>
</ul>
<h2 id="docker常见误区"><a href="#docker常见误区" class="headerlink" title="docker常见误区"></a>docker常见误区</h2><p>docker并非虚拟机，其本质相当与宿主机的一个进程。在使用docker容器的时候，我们一般一次只运行一个程序，<br>docker执行的程序相当于其第一个进程，当这个进程结束的时候。docker容器也就随之停止。</p>
<p>因此运行docker容器时一定要注意第一个运行的程序一结束，容器便会停止，比如<code>/bin/sh -c command</code>，此时<code>/bin/sh</code>是第一个程序，<br>当此程序运作完毕时，容器也会停止。</p>
<h2 id="docker常用指令"><a href="#docker常用指令" class="headerlink" title="docker常用指令"></a>docker常用指令</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker run 镜像名:tagname 指令1 &amp;&amp; [指令2 &amp;&amp; [指令3......]]</span><br></pre></td></tr></table></figure>

<p>从指定镜像运行一个容器，若本地没有则从docker hub下载制定镜像,<br>同时运行容器后的指令，可以通过<code>&amp;&amp;</code>连接符运行多个指令</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker tag local-image:tagname new-repo:tagname</span><br><span class="line">docker push new-repo:tagname</span><br></pre></td></tr></table></figure>

<p>也可以通过cli方式从本地推送自制的镜像至docker hub（类似于git仓库，可以设置公共仓库和私有仓库）。<br>不过因为类似一个云端存储，免费版docker用户公共镜像只能有一个</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker login</span><br></pre></td></tr></table></figure>

<p>此命令可以登录至docker hub，<br>之后可以通过docker push发布自己的镜像不过镜像名字必须为<code>用户名/镜像名:标签名</code></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker run -it 指定镜像 /bin/bash </span><br></pre></td></tr></table></figure>

<p>交互式形式在指定镜像上生成容器并在容器内与容器进行交互。<br><code>/bin/bash</code>指定使用的shell类型</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker search 镜像名</span><br></pre></td></tr></table></figure>

<p>在docker hub上查找有无公开的镜像</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker pull 镜像地址</span><br></pre></td></tr></table></figure>

<p>从docker hub上拉取公开的镜像</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker pull</span><br><span class="line">docker commit - 可以提交一个正在运行的容器，通过该容器生成一个我们已经配置好环境的镜像</span><br><span class="line">docker push</span><br></pre></td></tr></table></figure>

<p>这意味着我们可以像使用git一样使用docker处理、更新镜像、容器</p>
<p>docker同时为我们提供了dockerfile，类似一个脚本，在脚本内填入创建镜像需要的指令，docker会自动帮助我们构建所需镜像。</p>
<p>自动构建脚本，相当于自动处理脚本内容在容器内处理，我们再通过容器id提交一个我们自己的镜像（配置了我们的环境）</p>
<p>push 用户名&#x2F;docker镜像名:tag</p>
<p>其中<code>docker commit</code>可以帮助我们从一个容器中构建出我们自己的镜像，<br>例如<code>docker commit containerName imageName</code>可以帮助我们使用制定的容器生成自定义的镜像</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker build -t [父镜像名]:[镜像的tag] [Dockerfile所在的文件路径]</span><br></pre></td></tr></table></figure>

<p>与Cmake，make一样，docker可以从dockerfile（自动构建，描述了使用的镜像还有构建环境需要的目录）中自动构建出一个配置了我们所需要环境的镜像。</p>
<p><code>--no-cache</code>参数可以指定不使用缓存构建容器</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo cat ubuntu-14.04-x86_64-minimal.tar.gz | docker import - ubuntu:14.04</span><br></pre></td></tr></table></figure>

<p>从本地文件系统（本地系统镜像导入到docker镜像）</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker images</span><br><span class="line">docker ps / docker ps -a</span><br></pre></td></tr></table></figure>

<p>images列出当前所有镜像列表包括其id<br>ps列出当前实例化的容器以及其id，有助于我们构建自己的镜像</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker container ls -a</span><br></pre></td></tr></table></figure>

<p>列出当前所拥有的容器</p>
<p>需要注意，当我们在交互模式的容器中使用exit或者是ctrl+d退出时，容器会立即终止。<br>此时我们可以通过<code>docker container start container_id/container_name</code>的方式来重新运行这个容器（之前终端已经退出，之前终端内运行的服务也已被终止）。</p>
<p><code>docker container restart container_id/container_name</code>重新运行一个容器</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker rm container_id/container_name</span><br></pre></td></tr></table></figure>

<p>删除指定的容器</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker rmi image_id/image_name</span><br></pre></td></tr></table></figure>

<p>删除指定的镜像</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker run -P image</span><br></pre></td></tr></table></figure>

<p>运行一个镜像，随机映射端口</p>
<p>使用<code>docker run -p host_port:container_port image</code>可以指定分配端口，<br>即我们可以将容器的端口映射至宿主机的端口，通过访问宿主机端口访问容器指定的端口</p>
<p><code>docker run -d</code>，d参数可以使容器在后台运行</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker inspect container_id/container_name</span><br></pre></td></tr></table></figure>

<p>此命令可以获取容器的具体信息</p>
<p>若搭配管道符和文本处理工具可以高效地获取我们需要的信息，<br>比如<code>docker inspect container_id/container_name | grep IPAddress</code>可以获取容器在宿主机中被分配的id</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker run -v host_path/container_path</span><br></pre></td></tr></table></figure>

<p>通过这个命令将宿主机的目录与容器的指定目录进行连接，相当于进行挂载，有助于数据的保存，可以通过这个命令来在宿主机中保存我们需要的日志信息等。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker save -o &lt; target_image_name.tar &gt; &lt; current_image_name &gt;</span><br></pre></td></tr></table></figure>

<p>以压缩包保存已经拉取的镜像</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker load -i &lt; target_image_name.tar &gt;</span><br></pre></td></tr></table></figure>

<p>加载本地镜像（压缩包形式）</p>
<h2 id="命令行使用"><a href="#命令行使用" class="headerlink" title="命令行使用"></a>命令行使用</h2><p>docker cli 的使用前需要先启动docker的守护进程</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo systemctl start docker</span><br><span class="line">sudo systemctl enable docker</span><br></pre></td></tr></table></figure>

<p>使用systemctl</p>
<h2 id="docker-一些小细节"><a href="#docker-一些小细节" class="headerlink" title="docker-一些小细节"></a>docker-一些小细节</h2><p>很多时候我们不用root账户来运行docker的相关服务，因为安全性很难得到保证。<br>因此我们需要使用普通用户来使用docker的服务。</p>
<p>在linux中，我们可以这么做：</p>
<ol>
<li>sudo gpasswd -a username docker</li>
<li>newgrp docker</li>
</ol>
<p>即将用户添加至docker组，这样可以免去使用sudo，通过root账户运行docker</p>
<p>在使用docker容器的时候，我们一些时候需要在交互模式下跑应用然后退出至我们的宿主机，很多时候我们并不想退出的时候关闭容器，因此我们可以使用快捷键<code>Ctrl+p+q</code>返回宿主机并保证容器能正常运行。</p>
<h2 id="singularity"><a href="#singularity" class="headerlink" title="singularity"></a>singularity</h2><p>和docker类似，singularity也提供了类似docker一样的容器服务，也有自己的<a href="https://cloud.sylabs.io/library">容器仓库</a>，  </p>
<p>不过singularity没有镜像的概念，我们运行的都是一个一个容器（容器和镜像不再单独分开）</p>
<p>singularity容器有两种模式（只读、沙盒）</p>
<p>sif文件：压缩后的只读的singularity镜像文件，生产使用主要形式<br>sandbox：可写的容器，常用于开发或者创建自己的容器</p>
<p>singularity目前常用于高性能计算中，其容器名后缀名.sif。</p>
<h3 id="singularity常用指令"><a href="#singularity常用指令" class="headerlink" title="singularity常用指令"></a>singularity常用指令</h3><p><strong>从外部源下载容器</strong></p>
<p>*<code>docker</code> 为docker的镜像仓库 <code>library</code> 为singularity的容器仓库</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">singularity -d build 容器名.sif 容器地址</span><br><span class="line">singularity pull 容器名.sif 容器地址</span><br></pre></td></tr></table></figure>

<p><strong>构建沙盒容器</strong></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">singularity build --sandbox 容器名 容器地址</span><br><span class="line">singularity shell -w[--writeable] 容器名</span><br><span class="line">singularity build 容器名.sif 容器本地地址/</span><br></pre></td></tr></table></figure>

<p><strong>通过Definition文件制作容器</strong></p>
<p>文件名.def</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">singularity build 容器名.sif def文件名.def</span><br></pre></td></tr></table></figure>

<p><strong>运行容器</strong></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">singularity exec 容器名.sif/simg bash -c &quot;指令&quot;</span><br><span class="line">singularity shell 容器名.sif - 交互式运行</span><br></pre></td></tr></table></figure>

<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><ul>
<li><a href="https://www.redhat.com/zh/topics/containers/whats-a-linux-container">https://www.redhat.com/zh/topics/containers/whats-a-linux-container</a></li>
<li><a href="https://www.cnblogs.com/bigben0123/p/7592970.html">https://www.cnblogs.com/bigben0123/p/7592970.html</a></li>
<li><a href="https://baijiahao.baidu.com/s?id=1614385619899407709&amp;wfr=spider&amp;for=pc">https://baijiahao.baidu.com/s?id=1614385619899407709&amp;wfr=spider&amp;for=pc</a></li>
<li><a href="https://www.kubernetes.org.cn/k8s">https://www.kubernetes.org.cn/k8s</a></li>
<li><a href="https://hub.docker.com/">https://hub.docker.com/</a></li>
<li><a href="https://docs.docker.com/get-started/overview/">https://docs.docker.com/get-started/overview/</a></li>
<li><a href="https://sylabs.io/">https://sylabs.io/</a></li>
<li><a href="https://www.jianshu.com/p/0882e65c5e0e">https://www.jianshu.com/p/0882e65c5e0e</a></li>
<li><a href="https://cloud.tencent.com/developer/article/1478616">https://cloud.tencent.com/developer/article/1478616</a></li>
<li><a href="http://www.xtaohub.com/Container-Tech/Singularity-in-nutshell.html">http://www.xtaohub.com/Container-Tech/Singularity-in-nutshell.html</a></li>
<li><a href="https://www.jianshu.com/p/8c40e2d78e1b">docker如何制作自己的镜像</a></li>
<li><a href="https://blog.csdn.net/qq_36274515/article/details/87910704">制作自己的Docker镜像</a></li>
<li><a href="https://yeasy.gitbook.io/docker_practice/container/stop">Docker–从入门到实践</a></li>
<li><a href="https://www.runoob.com/docker/docker-dockerfile.html">Dockerfile–菜鸟教程</a></li>
<li><a href="https://blog.csdn.net/u013870094/article/details/84929822">Docker 给运行中的容器设置端口映射的方法</a></li>
<li><a href="https://www.runoob.com/docker/docker-image-usage.html">Docker-镜像使用–菜鸟教程</a></li>
<li><a href="https://blog.csdn.net/weixin_45459911/article/details/108784341">如何退出docker但不关闭</a></li>
<li><a href="https://blog.csdn.net/dongdong9223/article/details/52998375">如何进入、退出docker的container</a></li>
</ul>
]]></content>
      <tags>
        <tag>容器技术</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker配置JupyterNoteBook服务器</title>
    <url>/2021/11/18/Docker%E9%85%8D%E7%BD%AEJupyterNoteBook%E6%9C%8D%E5%8A%A1%E5%99%A8/</url>
    <content><![CDATA[<h2 id="设计思路"><a href="#设计思路" class="headerlink" title="设计思路"></a>设计思路</h2><p>使用vps搭建jupyterNotebook服务器，设置域名和端口映射，已经映射容器内部目录与服务器目录</p>
<ul>
<li>配置域名解析至服务器的公网ip，这样我们可以通过域名+端口的方式连接至jupyterNotebook</li>
<li>设置端口映射，在运行构建好jupyter环境的docker容器时将其开启的端口与vps端口映射，以便访问</li>
<li>设置容器内部文件夹与服务器文件夹的映射，使得容器意外终止后内部数据不会被删除</li>
</ul>
<h2 id="设计流程"><a href="#设计流程" class="headerlink" title="设计流程"></a>设计流程</h2><p>安装docker-&gt;下载docker镜像-&gt;创建容器并配置相关环境-&gt;制成镜像并运行-&gt;配置网络并在浏览器中访问</p>
<p>或者使用dockerfile自动构建</p>
<h3 id="安装docker"><a href="#安装docker" class="headerlink" title="安装docker"></a>安装docker</h3><p>具体安装细节请参考<a href="https://www.docker.com/">官方文档</a></p>
<p>也可移步至本人的<a href="https://royenheart.com/2021/11/17/docker/">另一文档</a></p>
<h3 id="下载docker镜像"><a href="#下载docker镜像" class="headerlink" title="下载docker镜像"></a>下载docker镜像</h3><p>本人采用ubuntu系统作为运行jupyternootbook的系统</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_">$ </span><span class="language-bash">docker pull ubuntu:latest</span></span><br></pre></td></tr></table></figure>

<h3 id="创建容器并配置相关环境"><a href="#创建容器并配置相关环境" class="headerlink" title="创建容器并配置相关环境"></a>创建容器并配置相关环境</h3><p><code>docker run -it --name djs ubuntu:latest</code></p>
<p><code>$ cd &amp;&amp; wget </code></p>
<h3 id="制成镜像并运行"><a href="#制成镜像并运行" class="headerlink" title="制成镜像并运行"></a>制成镜像并运行</h3><p>首先更新系统<code>apt update</code>然后下载必要的软件<code>apt install wget vim</code>，<br>若使用dockerfile进行自动构建，可不下载vim</p>
<p>下载anaconda官网提供的shell脚本安装<a href="https://repo.anaconda.com/archive/Anaconda3-2021.11-Linux-x86_64.sh">conda环境</a></p>
<p>运行shell脚本安装conda（使用&#x2F;bin&#x2F;bash），当安装完毕后执行<code>conda init bash</code>并重启终端（exit），<br>执行conda create –name DJS &amp;&amp; conda activate DJS配置环境</p>
<p>通过conda环境安装jupyter：<code>conda install -c conda-forge jupyter-lab</code></p>
<p>生成jupyter-notebook的启动参数<code>jupyter-notebook --generate-config</code></p>
<p>修改<code>~/.jupyter/jupyter_notebook_config.py</code>文件并添加以下参数：</p>
<ol>
<li>c.NotebookApp.ip &#x3D; ‘<em>‘ - 配置允许远程访问的ip地址，</em>表示任何地址均有权限。可自行修改成想要开放的ip地址</li>
<li>c.NotebookApp.notebook_dir &#x3D; dir_name - 配置jupyter-notebook的工作目录，在此目录下修改、添加程序文件</li>
<li>c.NotebookApp.token &#x3D; ‘’ - 配置访问的token，也可以设置访问的密码</li>
</ol>
<p>创建dir_name目录，作为jupyter-notebook的工作目录</p>
<p>至此容器环境设置完毕，之后可通过<code>docker build</code>命令加上对应参数从容器生成镜像</p>
<h3 id="配置网络并在浏览器中访问"><a href="#配置网络并在浏览器中访问" class="headerlink" title="配置网络并在浏览器中访问"></a>配置网络并在浏览器中访问</h3><p>假设生成的镜像名字是djs:v1，我们可以通过<br><code>docker run -dit --name containerName -p 7777:8888 -v /hostWorkDir:/dir_name djs:v1 jupyter-notebook</code><br>后台运行docker容器，同时开启jupyter-notebook服务。<br>此命令将容器的8888端口（jupyter-notebook开启的默认端口）与宿主机的7777端口进行映射，<br>同时将宿主机的hostWorkDir目录挂载至容器中jupyter-notebook服务的工作目录，做到数据不在容器中存储。</p>
<p>要在浏览器中访问，我们需要能通过ip、域名访问到服务器并开启7777端口</p>
<p>可以通过<code>http://localhost:7777</code>本地访问</p>
<p>若要设置ssl证书通过https协议进行访问，需要提供证书并修改相关参数</p>
<blockquote>
<p>不推荐使用root账户运行服务，建议开一个新的普通用户开启jupyter-notebook服务</p>
</blockquote>
<h2 id="一些高效指令"><a href="#一些高效指令" class="headerlink" title="一些高效指令"></a>一些高效指令</h2><ol>
<li>批量删除编译错误或未指定名字的实验镜像</li>
</ol>
<p><code>docker rmi $(docker images | grep &quot;&lt;none&gt;&quot; | awk &#39;//&#123;print $3&#125;&#39;)</code></p>
<p>这个命令同样使用于已经退出的容器，只需修改为</p>
<p><code>docker rm $(docker container ls -a | grep &quot;Existed&quot; | awk &#39;//&#123;print $1&#125;&#39;)</code></p>
<hr>
<h2 id="资料引用"><a href="#资料引用" class="headerlink" title="资料引用"></a>资料引用</h2><ul>
<li><a href="https://blog.csdn.net/Ezreal_King/article/details/77285366">批量删除镜像</a></li>
<li><a href="https://blog.csdn.net/whatday/article/details/104048064">docker不使用缓存重建镜像</a></li>
<li><a href="https://blog.csdn.net/weixin_35959421/article/details/113537332">docker的备份和数据迁移</a></li>
<li><a href="http://dockone.io/article/131">docker最佳实践</a></li>
<li><a href="https://blog.csdn.net/haeasringnar/article/details/82079943">Ubuntu安装anaconda 介绍、安装、配置</a></li>
<li><a href="https://blog.csdn.net/eswai/article/details/79437428">利用Docker环境配置jupyter notebook服务器</a></li>
<li><a href="https://blog.csdn.net/qq_35203425/article/details/103223892">使用Anaconda激活python3.6环境出错conda init SHELL_NAME解决</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/147995194">写 Dockerfile 的一些技巧</a></li>
<li><a href="https://qastack.cn/programming/30716937/dockerfile-build-possible-to-ignore-error">Dockerfile构建-可以忽略错误吗？</a></li>
</ul>
]]></content>
      <tags>
        <tag>容器技术</tag>
        <tag>python学习与技术</tag>
      </tags>
  </entry>
  <entry>
    <title>VimLearn</title>
    <url>/2021/11/16/VimLearn/</url>
    <content><![CDATA[<blockquote>
<p>Just for personal learning and some notes</p>
</blockquote>
<hr>
<h2 id="What"><a href="#What" class="headerlink" title="What"></a>What</h2><p>Vim is a mode text editor which has many modes<br>including Normal Mode, Insert Mode etc.</p>
<p>Each mode has its own functions and operations.</p>
<p>The normal mode in Vim is like a language, that we have different operations,<br>hava different concepts and commands,<br>and we can combine them to better edit our file in command line mode.</p>
<ul>
<li>the conclusion from the <a href="https://youtu.be/a6Q8Na575qc">video</a> that introduces the Vim</li>
</ul>
<p>The combination of commands are like [extra_param][command]</p>
<h2 id="Mode"><a href="#Mode" class="headerlink" title="Mode"></a>Mode</h2><ul>
<li>Normal Mode -&gt; with key ‘i’ -&gt; Insert Mode</li>
<li>Normal Mode -&gt; with key ‘R’ -&gt; Replace Mode</li>
<li>Normal Mode -&gt; with key ‘v’ -&gt; Visual Mode</li>
<li>Normal Mode -&gt; with key ‘Shift V’ -&gt; Visual Mode lmo</li>
<li>Normal Mode -&gt; with key ‘Ctrl V’ -&gt; Visual Mode block</li>
<li>Normal Mode -&gt; with key ‘:’ -&gt; Command-line Mode</li>
</ul>
<h2 id="Vim-Command"><a href="#Vim-Command" class="headerlink" title="Vim Command"></a>Vim Command</h2><p>‘:help command’ - list the introductions of the command</p>
<p>We should know that in Vim, the command ‘:w’ and the command ‘w’ is different.<br>The first is the vim command, and the second is just a command in normal mode</p>
<p>When we enter the introduction, we can use ‘:q’ to quit and back to our open file.<br>Like we open another file which includes the introductions of the command.</p>
<p>‘:w’ - save the file</p>
<p>‘:q’ or ‘:quit’ - quit the current window not the whole vim</p>
<p>In vim, there is concept of ‘window’ such as when we use ‘:sp’ command to split window,<br>or use ‘:help’ to open the window which contents the introduction of the command.<br>When we open this window, we can use ‘:q’ to quit them and back to our open file.</p>
<p>‘:qa’ - quit all, this is the command to quit the vim, or we can say, quit all windows.</p>
<p>‘:set number’ - set line number</p>
<p>This is a vim command to put line number to better edit the file,<br>the usage of this we can use ‘: set number’ to temporary set the number,<br>once we need to put line number every time we open the file with vim,<br>we can add a file named ‘.vimrc’ (vim reconfigure) in our home directory ‘~&#x2F;.vimrc’,<br>and put ‘set number’ to it, then next time we use vim we will find the line number.</p>
<p>‘:sp’ - split screen, split the current text screen so we can see mutil screens that shows the text.</p>
<p>‘:set sw’ - set the spaces which indent includes</p>
<p>‘&#x2F;search’ - search command ‘&#x2F;‘ can help us move to the position of searched words.<br>For example, ‘&#x2F;is’ is to search the words “is”</p>
<h2 id="Movement"><a href="#Movement" class="headerlink" title="Movement"></a>Movement</h2><blockquote>
<p>these movements command can be used in normal mode</p>
</blockquote>
<p>‘j’ - moves down<br>‘K’ - moves up<br>‘h’ - moves left<br>‘g’ - moves right<br>‘b’ - moves one word backward<br>‘e’ - moves to the end of a word<br>‘0’ - moves to the head of the line<br>‘^’ - the same effect as ‘0’ does<br>‘$’ - moves to the end of the line<br>‘^U’ - hold up, switch to up page<br>‘^D’ - hold down, swicth to down page<br>‘G’ - moves to the end of the buffers(file)<br>‘gg’ - moves to the head of the buffers(file)<br>‘L’ - Lowest, moves to the end of current page<br>‘M’ - Middle, moves to the middle of current page<br>‘H’ - Highest, moves to the top of current page<br>‘F[word to be found]’ - find the fisrt word that equals to the find param we give,<br>and the moves to that place. Note that this command’s range is limited to the current line.</p>
<p>Like we see in the conclusion,<br>this command abstracts our moving operations as commands in normal mode.</p>
<p>Thus we can combine these with other operations to help us move faster in the file,<br>such ‘3j’ command in normal mode can help us move 3 lines one time.</p>
<h2 id="Text-Command"><a href="#Text-Command" class="headerlink" title="Text Command"></a>Text Command</h2><h3 id="Special-Text"><a href="#Special-Text" class="headerlink" title="Special Text"></a>Special Text</h3><p>‘^’ - The start of one line<br>‘$’ - The end of one line</p>
<h3 id="Normal-Mode"><a href="#Normal-Mode" class="headerlink" title="Normal Mode"></a>Normal Mode</h3><p>‘X’ - Delete one Character where cursor is on.</p>
<p>‘v’ + cursor move - select the text you have selected</p>
<p>‘y(yank)’ - copy the select text</p>
<p>It’s a command which we can use it to copy the text, we can also use ‘yy’ to copy the whole line.<br>‘[number]yy’ is a special command which can help us copy the next [number] lines from the current line(includes the current line).<br>‘yw’ can copy the current word.</p>
<p>‘p(paste)’ - paste the copy line to the current position</p>
<p>‘o’ - add a new line after the line where the cursor locates.</p>
<p>‘O’ - add a new line as command ‘o’ and change into the insert mode</p>
<p>‘dd’ - delete the current line</p>
<p>‘u’ - undo the last change you did including actions in insert mode</p>
<p>‘di[parentheses]’ - for example, when cursor is in words “[htt&#x2F;&#x2F;sfsdfdfa]”,<br>we can use ‘di[‘ command to delete the content which is included in the “[]”</p>
<p>‘da[parentheses]’ - delete all include the parenthese itself</p>
<h2 id="Visual-Mode"><a href="#Visual-Mode" class="headerlink" title="Visual Mode"></a>Visual Mode</h2><p>When we enter the key ‘v’ in normal mode, we will be in the state of ‘Visual Mode’.<br>This mode can help us select mutil words, then we can use ‘y’ or ‘p’ to operate on these words.<br>Or we can just operate one word one time.</p>
<p>‘V’ - when we use this command in Visual Mode or Normal Mode,<br>we can now select a whole line once we move</p>
<p>‘^V’ - when we use this command, we will enter the ‘visual block mode’,<br>which we can select a rectangular blocks of text.</p>
<h2 id="Special-Command"><a href="#Special-Command" class="headerlink" title="Special Command"></a>Special Command</h2><p>^V : Ctrl-V : <C-V></p>
<h2 id="Expand-Reading"><a href="#Expand-Reading" class="headerlink" title="Expand Reading"></a>Expand Reading</h2><p>Vim offers 12 clipboards which are “0,”1,”2……”9,”” ,<br>we can use ‘:reg’ in normal to check the content in clipboard,<br>the default clipboard is “” and “0,<br>we can use ‘“Ny’ in Visual Mode(^V) to copy the selected text to a clipboard</p>
<p>In conclusion, we can place ‘p’, ‘y’ or something else with ‘“Np’…,<br>the meaning is that we can copy or paste to and from different clipboards.</p>
<hr>
<h2 id="Settings-for-VIM"><a href="#Settings-for-VIM" class="headerlink" title="Settings for VIM"></a>Settings for VIM</h2><p>There are ways to set the VIM:</p>
<ol>
<li>System setting files are located in <code>/etc/.vimrc</code></li>
<li>User setting files are located in <code>~/.vimrc</code> (priority is higher than system)</li>
</ol>
<p>We can use command <code>:set all</code> in VIM to check the settings. </p>
<h2 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h2><ul>
<li><a href="https://liushiming.cn/article/copy-and-paste-in-vim.html">About Vim Copy and Paste</a></li>
<li><a href="http://c.biancheng.net/view/3024.html">Some usual VIM settings</a></li>
</ul>
]]></content>
      <tags>
        <tag>Vim学习与使用</tag>
      </tags>
  </entry>
  <entry>
    <title>使用EnvironmentModule管理linux环境</title>
    <url>/2021/11/20/%E4%BD%BF%E7%94%A8EnvironmentModule%E7%AE%A1%E7%90%86linux%E7%8E%AF%E5%A2%83/</url>
    <content><![CDATA[<blockquote>
<p>在linux下环境设置是一个很重要的配置环节，特别是在多用户的情况下</p>
</blockquote>
<p>先上<a href="https://modules.readthedocs.io/en/latest/">官方教程</a></p>
<h1 id="Environment-Module"><a href="#Environment-Module" class="headerlink" title="Environment Module"></a>Environment Module</h1><p>简化shell初始化的工具。在设置环境的时候，<br>我们一般使用脚本批量设置或者主动在shell配置环境内设置环境</p>
<p>Environment Module 使用modulefile文件进行环境的管理，相当于把环境设置模块化，<br>这样我们在进行不同用户，不同节点的环境设置时可以分别引入不同的模块管理，<br>简化工作。</p>
<h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><p>上<a href="http://modules.sourceforge.net/">官网</a>下载源代码进行编译安装</p>
<p>下载得到源代码modules-xxx</p>
<p>解压进行安装</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt install -y autoconf tclsh tcl-dev tk-dev mesa-common-dev libjpeg-dev libtogl-dev</span><br><span class="line">tar -xvf modules-xxx</span><br><span class="line"></span><br><span class="line">cd modules-xxx</span><br><span class="line">./configure</span><br><span class="line">make</span><br><span class="line">make install</span><br></pre></td></tr></table></figure>

<h2 id="设置环境变量"><a href="#设置环境变量" class="headerlink" title="设置环境变量"></a>设置环境变量</h2><p>Environt-Module为多个Shell解释器提供了环境变量设置脚本，需要使用这些脚本初始化软件的环境变量</p>
<p><code>source /InstallFolder/modules-xxx/init/bash</code></p>
<p>可以添加到bashrc</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">echo &quot;source /path/to/modules-xxx/init/bash&quot; &gt;&gt; ~/.bashrc</span><br><span class="line">source ~/.bashrc</span><br></pre></td></tr></table></figure>

<p>为bash设置module，也可以为其他shell设置，<br>init里面提供了针对多种shell设置其应用环境变量的脚本</p>
<p>之后可执行<code>module</code>命令进行环境变量的维护。</p>
<h2 id="设置模块"><a href="#设置模块" class="headerlink" title="设置模块"></a>设置模块</h2><blockquote>
<p>$ module avail</p>
</blockquote>
<p>列出当前可用模块</p>
<blockquote>
<p>$ module load use.own</p>
</blockquote>
<p>加载自带模块<code>use.own</code>，加载后会在用户家目录建立<code>privatemodules</code>目录，<br>之后可在此目录下添加自定义的模块配置文件</p>
<blockquote>
<p>$ module list</p>
</blockquote>
<p>列出当前已经加载的模块</p>
<blockquote>
<p>$ module load </p>
</blockquote>
<p>加载指定模块</p>
<blockquote>
<p>$ module unload </p>
</blockquote>
<p>卸载指定模块</p>
<blockquote>
<p>$ module help</p>
</blockquote>
<p>列出帮助</p>
<blockquote>
<p>$ module use &lt; folder &gt;</p>
</blockquote>
<p>增加模块查找的目录，使用<code>module avail</code>将列出增加的目录下可用的模块，不使用时使用命令<code>module unuse &lt; folder &gt;</code>即可。</p>
<h2 id="自定义模块"><a href="#自定义模块" class="headerlink" title="自定义模块"></a>自定义模块</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_">#</span><span class="language-bash">%Module -*- tcl -*-</span></span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">显示 module <span class="built_in">help</span> 主要内容</span></span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash"><span class="comment"># 设置之后可以使用 module help xxx 列出此模块的帮助内容</span></span></span><br><span class="line">proc ModulesHelp &#123; &#125; &#123;</span><br><span class="line">        puts stderr &quot;main contents&quot; </span><br><span class="line">&#125;</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">显示 module whatis 主要内容</span></span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash"><span class="comment"># 设置之后可以使用 module whatis xxx 列出此模块的信息</span></span></span><br><span class="line">module-whatis &quot;main contents&quot;</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">module 此模块加载前需要加载的模块类，加载失败会报错</span></span><br><span class="line">prereq xxx</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">module 声明与此模块冲突的模块，也可以是存放冲突模块的文件名</span></span><br><span class="line">conflict xxx</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">加载此模块的同时加载其他模块</span></span><br><span class="line">module load xxx</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">设置环境变量（设置为xxx）</span></span><br><span class="line">setenv something xxx</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">添加环境变量（添加xxx）</span></span><br><span class="line"></span><br><span class="line">append-path PATH xxx</span><br></pre></td></tr></table></figure>

<h2 id="MODULEPATH"><a href="#MODULEPATH" class="headerlink" title="MODULEPATH"></a>MODULEPATH</h2><p>此环境变量用于设置module查找模块的路径，若通过包管理器默认安装，可于<code>/usr/share/modules/init/bash</code>文件中添加：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">export MODULEPATH=xxx:xxx</span><br></pre></td></tr></table></figure>

<p>该语句设置后source该文件可重新设置module查找模块路径</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_">#</span><span class="language-bash"><span class="comment"># 使用envrionment module运行环境变量设置脚本配置环境</span></span></span><br><span class="line"></span><br><span class="line">module实质为tcl脚本文件，无法运行shell命令</span><br><span class="line"></span><br><span class="line">因此无法通过`source xxx.sh`脚本的方式添加环境变量</span><br><span class="line"></span><br><span class="line">可以先获取xxx.sh内定义的环境变量，再加到我们的模块文件中</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="资料引用"><a href="#资料引用" class="headerlink" title="资料引用"></a>资料引用</h2><ul>
<li><a href="https://sourceforge.net/projects/env2/">Environment Modules 简明教程</a></li>
<li><a href="https://modules.readthedocs.io/en/latest/">Environment Modules 官方文档</a></li>
</ul>
]]></content>
      <tags>
        <tag>linux学习与技术</tag>
      </tags>
  </entry>
  <entry>
    <title>使用Perf工具分析程序性能</title>
    <url>/2021/12/08/%E4%BD%BF%E7%94%A8Perf%E5%B7%A5%E5%85%B7%E5%88%86%E6%9E%90%E7%A8%8B%E5%BA%8F%E6%80%A7%E8%83%BD/</url>
    <content><![CDATA[<p>仅个人学习与记录</p>
<h1 id="perf"><a href="#perf" class="headerlink" title="perf"></a>perf</h1><p>perf是Linux内核提供的一个性能分析工具，可以跟踪程序的运行并分析其各种性能比如CPU负载、内存占用等</p>
<h2 id="安装-x2F-开启perf"><a href="#安装-x2F-开启perf" class="headerlink" title="安装&#x2F;开启perf"></a>安装&#x2F;开启perf</h2><ol>
<li>debian</li>
</ol>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt install linux-perf</span><br><span class="line">sudo apt install linux-perf-$(uname -r)</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>ubuntu</li>
</ol>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt install linux-tools-$(uname -r) linux-cloud-tools-$(uname -r)</span><br></pre></td></tr></table></figure>

<h2 id="一些使用问题"><a href="#一些使用问题" class="headerlink" title="一些使用问题"></a>一些使用问题</h2><ol>
<li>Accesss to performance monitoring and observability operations is limited</li>
</ol>
<p>解决方法：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo sysctl -w kernel.perf_event_paranoid=-1</span><br></pre></td></tr></table></figure>

<h2 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a>常用命令</h2><blockquote>
<p>perf stat ls - 打印出程序使用硬件的情况</p>
</blockquote>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul>
<li><a href="https://blog.eastonman.com/blog/2021/02/use-perf/">Easton’s Man’s Blog</a></li>
</ul>
]]></content>
      <tags>
        <tag>Linux学习与技术</tag>
      </tags>
  </entry>
  <entry>
    <title>正则表达式自查</title>
    <url>/2021/11/16/%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F%E8%87%AA%E6%9F%A5/</url>
    <content><![CDATA[<h1 id="正则表达式"><a href="#正则表达式" class="headerlink" title="正则表达式"></a>正则表达式</h1><ul>
<li><code>regular expression</code></li>
</ul>
<p>正则表达式通过元字符+普通字符组成的字符串来匹配目标字符串中符合表达式规则的子串，常用于字符串的修改，子串的提取等</p>
<blockquote>
<p>仅为个人学习记录，一般都是现查现用（</p>
</blockquote>
<h2 id="普通字符"><a href="#普通字符" class="headerlink" title="普通字符"></a>普通字符</h2><p>包括没有显式指定为元字符的所有可打印和不可打印的字符，包括所有大写、小写字母，所有数字，所有标点符号和其他一些符号</p>
<h2 id="非打印字符"><a href="#非打印字符" class="headerlink" title="非打印字符"></a>非打印字符</h2><p>\f 换页符</p>
<h2 id="匹配"><a href="#匹配" class="headerlink" title="匹配"></a>匹配</h2><p>[]内表示需要被匹配的字符，需要注意正则表达式实际上是一个字符一个字符的进行匹配，添加适当的规则（元字符+待匹配字符）可以实现匹配子串等作用，而这元字符和待匹配字符构成的规则可以称为一个表达式，即方括号内语句，”中括号“表达式，0-9表示的其实是0-9中的<strong>一个</strong>字符</p>
<h2 id="x3D-lt-x3D-lt-gt"><a href="#x3D-lt-x3D-lt-gt" class="headerlink" title="?&#x3D; ?&lt;&#x3D; ?! ?&lt;!&gt;"></a>?&#x3D; ?&lt;&#x3D; ?! ?&lt;!&gt;</h2><p><code>exp1(?=exp2)</code> 查找exp2前面的exp1</p>
<p><code>(?&lt;=exp2)exp1</code> 查找exp2后面的exp1</p>
<p><code>exp1(?!exp2)</code> 查找后面不是exp2的exp1</p>
<p><code>(?&lt;!exp2)exp1</code> 查找前面不是exp2的exp1</p>
<h2 id="运算符优先级"><a href="#运算符优先级" class="headerlink" title="运算符优先级"></a>运算符优先级</h2><table>
<thead>
<tr>
<th>匹配符号</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td>\</td>
<td>转义符</td>
</tr>
<tr>
<td>(), (?: ), (?&#x3D;), []</td>
<td>圆括号和方括号</td>
</tr>
<tr>
<td>*, +, ?, {n}, {n,}, {n,m}</td>
<td>限定符</td>
</tr>
<tr>
<td>^, $, \任何元字符、任何字符</td>
<td>定位点和序列（即：位置和顺序）</td>
</tr>
<tr>
<td></td>
<td></td>
</tr>
<tr>
<td>字符具有高于替换运算符的优先级，使得”m</td>
<td>food”匹配”m”或”food”。若要匹配”mood”或”food”，请使用括号创建子表达式，从而产生”(m</td>
</tr>
</tbody></table>
]]></content>
      <tags>
        <tag>正则表达式</tag>
      </tags>
  </entry>
  <entry>
    <title>计算机对数的计算</title>
    <url>/2022/05/13/%E8%AE%A1%E7%AE%97%E6%9C%BA%E5%AF%B9%E6%95%B0%E7%9A%84%E8%AE%A1%E7%AE%97/</url>
    <content><![CDATA[<blockquote>
<p>个人计算机组成原理学习记录 </p>
</blockquote>
<h2 id="各种数的表示方法"><a href="#各种数的表示方法" class="headerlink" title="各种数的表示方法"></a>各种数的表示方法</h2><ol>
<li>原码（机器码），以此表示的数最高位不代表数值而是符号，运算时符号位和数值位分开运算</li>
<li>反码，负数时原码除符号位外取反，正数不变，运算时（+&#x2F;-）符号位和数值位可一起进行运算，但会产生-0和+0</li>
<li>补码，负数时原码除符号位外取反+1，正数不变，运算时（+&#x2F;-）符号位和数值位可一起进行运算，符号位即可表示符号又可以当作数值位进行运算，表示范围多一个（4位可表示-8~7）</li>
<li>移码&#x2F;增码，补码最高位取反，最高位1表示正数，0表示负数。这样做可以使移码和原码（真值）在$-2^{n-1}$ ~ $2^{n-1}-1$区间呈线性正比关系，本质上是补码+偏置值，对于8位补码，偏置值为$2^{7}$。另一种移码和真值在$-2^{n-1}+1$ ~ $2^{n-1}-1$区间呈线性正比关系，偏置值是$2^{n-1} - 1$，这样的好处是留出指数$-2^{n-1}$（指数全为1）和$-2^{n-1}+1$（指数全为0）用作其他用途（特殊值）。</li>
<li>规格化浮点数，类似科学计数法：$+&#x2F;-1.xxxxx_2 x 2^{E}$，1隐含，不用写在尾数位。</li>
</ol>
<h3 id="浮点数规格化表示特殊值（对于计算异常等更方便）"><a href="#浮点数规格化表示特殊值（对于计算异常等更方便）" class="headerlink" title="浮点数规格化表示特殊值（对于计算异常等更方便）"></a>浮点数规格化表示特殊值（对于计算异常等更方便）</h3><ol>
<li>表示0：指数为0（负指数超出最小允许值）；尾数为0；符号位0为+0，1为-0</li>
<li>表示无穷：指数为1（正指数超出最大允许值）；尾数为0；符号位0为正无穷，1为负无穷。</li>
<li>表示非数（NaN）：指数为1；尾数非0；符号位0&#x2F;1</li>
</ol>
<h2 id="除法"><a href="#除法" class="headerlink" title="除法"></a>除法</h2><p>除法最重要的是比较（绝对值）的步骤，要以比较的结果来判断商取0还是1。</p>
<p>对于X，Y的绝对值比较，可以通过|x|和|y|取绝对值后比较，也可以通过x+y(异号)&#x3D;r&#x2F;x-y(同号)&#x3D;r的方法比较，当异号时r与y同号表示y的绝对值大，当同号时r与y同号表示y绝对值小。</p>
<p>并且注意，由于补码运算得到的商相当于以反码进行运算，因此x，y异号，商为负数，若够减时上商1会导致原码不正确，因此x，y异号够减时上商0。</p>
<p>补码运算最后的余数是以补码表示的。</p>
<p>在补码运算中，其绝对值比较就可以有下述的关系：</p>
<table style="border-collapse: collapse; border: none; border-spacing: 0px;">
    <caption>
        补码运算绝对值关系
    </caption>
    <tr>
        <td style="border-top: 2px solid rgb(0, 0, 0); border-bottom: 2px double rgb(0, 0, 0); padding-right: 3pt; padding-left: 3pt;">
            [x]_补与[y]_补
        </td>
        <td style="border-top: 2px solid rgb(0, 0, 0); border-bottom: 2px double rgb(0, 0, 0); padding-right: 3pt; padding-left: 3pt;">
            比较操作
            <br>
        </td>
        <td style="border-top: 2px solid rgb(0, 0, 0); border-bottom: 2px double rgb(0, 0, 0); padding-right: 3pt; padding-left: 3pt;">
            余数[r]_补与除数[y]_补
        </td>
        <td style="border-top: 2px solid rgb(0, 0, 0); border-bottom: 2px double rgb(0, 0, 0); padding-right: 3pt; padding-left: 3pt;">
            商
        </td>
        <td style="border-top: 2px solid rgb(0, 0, 0); border-bottom: 2px double rgb(0, 0, 0); padding-right: 3pt; padding-left: 3pt;">
            操作
        </td>
    </tr>
    <tr>
        <td rowspan="2" style="border-top: 2px double rgb(0, 0, 0); padding-right: 3pt; padding-left: 3pt;">
            同号
        </td>
        <td rowspan="2" style="border-top: 2px double rgb(0, 0, 0); padding-right: 3pt; padding-left: 3pt;">
            [x]_补+[-y]_补
        </td>
        <td style="border-top: 2px double rgb(0, 0, 0); padding-right: 3pt; padding-left: 3pt;">
            同号，表示够减，或者说y绝对值大
        </td>
        <td style="border-top: 2px double rgb(0, 0, 0); padding-right: 3pt; padding-left: 3pt;">
            1
        </td>
        <td style="border-top: 2px double rgb(0, 0, 0); padding-right: 3pt; padding-left: 3pt;">
            当作[x]_补，左移+[-y]_补
        </td>
    </tr>
    <tr>
        <td style="padding-right: 3pt; padding-left: 3pt;">
            异号，不够减
        </td>
        <td style="padding-right: 3pt; padding-left: 3pt;">
            0
        </td>
        <td style="padding-right: 3pt; padding-left: 3pt;">
            不恢复余数法左移+[y]_补
        </td>
    </tr>
    <tr>
        <td rowspan="2" style="border-bottom: 2px solid rgb(0, 0, 0); padding-right: 3pt; padding-left: 3pt;">
            异号
        </td>
        <td rowspan="2" style="border-bottom: 2px solid rgb(0, 0, 0); padding-right: 3pt; padding-left: 3pt;">
            [x]_补+[y]_补
        </td>
        <td style="padding-right: 3pt; padding-left: 3pt;">
            同号，不够减
        </td>
        <td style="padding-right: 3pt; padding-left: 3pt;">
            1
        </td>
        <td style="padding-right: 3pt; padding-left: 3pt;">
            不恢复余数法左移+[-y]_补
        </td>
    </tr>
    <tr>
        <td style="border-bottom: 2px solid rgb(0, 0, 0); padding-right: 3pt; padding-left: 3pt;">
            异号，够减
        </td>
        <td style="border-bottom: 2px solid rgb(0, 0, 0); padding-right: 3pt; padding-left: 3pt;">
            0
        </td>
        <td style="border-bottom: 2px solid rgb(0, 0, 0); padding-right: 3pt; padding-left: 3pt;">
            当作[x]_补，左移+[y]_补
        </td>
    </tr>
</table>

<p>实际运算中商的精度一般与x，y精度相同，余数最后乘上$2^{-n}$，n为左移次数</p>
<h3 id="商的校正"><a href="#商的校正" class="headerlink" title="商的校正"></a>商的校正</h3><p>前面商的取值实际上是按照反码的方式进行的，当商最后得出为负，则会因反码与补码之间末位1的区别导致误差，因此有下列两种方式解决：</p>
<ol>
<li>末位恒置1法</li>
</ol>
<p>最后的商末位固定为1，即运算至最后的位数时，此时余数左移，商末位固定为1，除法结束</p>
<ol start="2">
<li>校正法</li>
</ol>
<p>即运算至最后的位数时，仍按表格进行正常操作得到余数和商，之后商若为负，则加$2^{-n}$矫正，n为左移的次数，余数若和除数补码呈不够减的关系，则需要恢复余数，对于x、y同号，则加上[y]_补，若x、y异号，加上[-y]_补</p>
<h2 id="浮点数运算"><a href="#浮点数运算" class="headerlink" title="浮点数运算"></a>浮点数运算</h2><h3 id="运算概念"><a href="#运算概念" class="headerlink" title="运算概念"></a>运算概念</h3><ol>
<li>舍入，类似十进制计算的四舍五入</li>
<li>对阶，使两数阶码相等以方便运算</li>
<li>规格化，使浮点数符合科学计数法（$(1.xxx)_2 x 2^{E-127}$）</li>
</ol>
<h3 id="浮点数计算实质"><a href="#浮点数计算实质" class="headerlink" title="浮点数计算实质"></a>浮点数计算实质</h3><p>规格化浮点数：</p>
<p>$A&#x3D;M_a \cdot 2^{E_a}$ 和 $B&#x3D;M_b \cdot 2^{E_b}$</p>
<ol>
<li>$A \pm B &#x3D; (M_a \pm M_b \cdot 2^{-(E_a-E_b)}) \cdot 2^{E_a}$</li>
<li>$A * B &#x3D; (M_a * M_b) \cdot 2^{E_a + E_b}$</li>
<li>$A &#x2F; B &#x3D; (M_a &#x2F; M_b) \cdot 2^{E_a - E_b}$</li>
</ol>
<h3 id="浮点数运算可能结果"><a href="#浮点数运算可能结果" class="headerlink" title="浮点数运算可能结果"></a>浮点数运算可能结果</h3><ol>
<li>阶码上溢：无穷。</li>
<li>阶码下溢：0。</li>
<li>尾数溢出：尾数最高有效位进位，则右规将进位加入，指数增加。此时计算的尾数（补码表示）连同符号位右移，将移出的位放入附加位作为</li>
<li>非规格化尾数：数值部分高位0，左规，指数减少。浮点数运算时加上隐含的1，若运算后高位为0，就进行左规。左规尾数左移，附加位移出对应位数至尾数</li>
<li>右规、对阶有效位缺失：类似四舍五入，有效位优先，进行尾数舍入。参考<a href="#%E8%88%8D%E5%85%A5">舍入规则</a></li>
</ol>
<p>上溢和下溢也是为什么指数使用移码且偏置常量为$2^{n-1} - 1$的原因。</p>
<h3 id="附加位"><a href="#附加位" class="headerlink" title="附加位"></a>附加位</h3><p>为提高浮点数精度，IEEE754规定附加位（中间结果右边加2个附加位）：</p>
<ol>
<li>Guard（保护位）：significand（mantissa，尾数）右边的位</li>
<li>Round（舍入位）：保护位右边的位</li>
<li>Stick（粘连位）：舍入位右边有任何非0数，粘连位为1，否则0。</li>
</ol>
<p>作用：</p>
<ol>
<li>保护对阶右移、运算中间结果。</li>
<li>左规使移到尾数。</li>
<li>舍入依据。</li>
</ol>
<h3 id="舍入"><a href="#舍入" class="headerlink" title="舍入"></a>舍入</h3><p>用于计算最后进行舍入</p>
<p>IEEE 754规定的浮点数右规只需要1次。</p>
<p>舍入规则（看附加位）：</p>
<ul>
<li>01：舍</li>
<li>11：入</li>
<li>10：强迫结果偶数</li>
</ul>
<ol>
<li>就近舍入（0舍1入法）</li>
<li>朝正无穷舍入（离待舍入尾数最近的数）</li>
<li>朝负无穷舍入</li>
<li>朝0方向舍入：直接截去</li>
</ol>
<h2 id="溢出时判断和处理（尾数补码表示，看符号位）"><a href="#溢出时判断和处理（尾数补码表示，看符号位）" class="headerlink" title="溢出时判断和处理（尾数补码表示，看符号位）"></a>溢出时判断和处理（尾数补码表示，看符号位）</h2><ol>
<li>单符号位：Cf最高数值位进位，Cs符号位进位。相同时结果正确，不同时溢出。</li>
<li>双符号位：第一符号位$S_{f1}$和第二符号位$S_{f2}$不相同溢出。最高符号位$S_{f1}$代表真正的符号。</li>
</ol>
]]></content>
      <tags>
        <tag>计算机组成原理</tag>
      </tags>
  </entry>
</search>
